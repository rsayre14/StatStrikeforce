apiVersion: apps/v1
kind: Deployment
metadata:
  name: machinelearning
  labels:
    role: app
spec:
  replicas: 2
  selector:
    matchLabels:
      role: app
  template:
    metadata:
      labels:
        role: app
    spec:
      containers:
      - name: machinelearning
        image: rsayre14/csc468:machinelearning
        resources:
          requests:
            cpu: 10m
        ports:
        - containerPort: 80
